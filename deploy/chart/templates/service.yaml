apiVersion: v1
kind: Service
metadata:
  name: {{ include "c6-hackathon.fullname" . }}
  labels:
    {{- include "c6-hackathon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "c6-hackathon.selectorLabels" . | nindent 4 }}
